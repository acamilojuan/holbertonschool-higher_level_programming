IMPORT
    >>> MyList = __import__('1-my_list').MyList

TESTS

Test 1: Checks empty list
    >>> temp = MyList()
    >>> print(temp)
    []
    >>> temp.print_sorted()
    []

Test 2: Checks print
    >>> my_list = MyList()
    >>> my_list.append(6)
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(3)
    >>> print(my_list)
    [6, 1, 4, 3]
    >>> my_list.print_sorted()
    [1, 3, 4, 6]
    >>> print(my_list)
    [6, 1, 4, 3]

Test 3: issubclass(type(object), list)
    >>> temp = MyList()
    >>> issubclass(type(temp), list)
    True

Test 4: isinstance(object, list)
    >>> temp = MyList()
    >>> isinstance(temp, list)
    True

Test 5: Negative numbers in a list
    >>> temp = MyList()
    >>> temp.append(-3)
    >>> temp.append(-18)
    >>> temp.append(-2)
    >>> print(temp)
    [-3, -18, -2]
    >>> temp.print_sorted()
    [-18, -3, -2]
    >>> print(temp)
    [-3, -18, -2]
